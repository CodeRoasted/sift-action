{
  "name": "sift-action",
  "version": "1.4.1",
  "private": true,
  "description": "Sift — structural diff of your CI logs, as a sticky PR comment + advisory gate.",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "tsc && node --test lib/tests/",
    "package": "ncc build src/main.ts -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/artifact": "^2.1.0",
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^6.0.0",
    "adm-zip": "^0.5.12"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.5",
    "@types/node": "^20.12.0",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.5"
  }
}
